using System;

namespace Project {
    internal class Program {
        static void Main(string[] args) {
            /* Operadores de Atribuição */

            // No C#, possuímos os seguintes operadores de atribuição:
            // = -> atribui
            // += -> soma e atribui
            // -= -> subtrai e atribui
            // *= -> multiplica e atribui
            // /= -> divide e atribui
            // %= -> obtem o resto da divisão e atribui

            /* Operadores de Atribuilão / Aritméticos */

            // ++variavel | variavel++ -> atribui mais 1
            // --variavel | variavel-- -> atribui menos 1
        }
    }
}
